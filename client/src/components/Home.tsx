import React from 'react';

const Home: React.FC = () => {
  return (
    <div className="container mt-5">
      <h1>Order Submission Tool</h1>
      <p>This tool is used for submitting orders for processing via a webpage</p>
    </div>
  );
};

export default Home;
